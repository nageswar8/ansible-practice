- name: install packages
  hosts: local
  connection: local
  tasks:
    - name: installing
      ansible.builtin.package:
        name: "{{item}}"
        state: present
      loop:
        - nginx
        - mysql
        - zip
  become: yes
