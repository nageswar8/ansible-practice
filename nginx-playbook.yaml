- name: "Nginx install play book"
  hosts: backend
  become: yes
  tasks:
    - name: "install nginx"
      dnf:
        name: nginx
        state: present

    - name: "check service started"
      service:
        name: nginx
        state: started
        enabled: yes
